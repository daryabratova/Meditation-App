import { createAction } from 'redux-act';

export const setTimeInterval = createAction('set time interval');
export const toggleTimer = createAction('toggle timer');
export const setTimePassed = createAction('set time passed');
export const resetTimer = createAction('reset timer');
export const setTheme = createAction('set theme');
